// Next.js bindings used by this template.
// Add APIs here when a page actually needs them. Extract a shared package
// only if another app needs the same surface.

module Metadata = {
  type metadata = {
    title: option<string>,
    description: option<string>,
  }
}
